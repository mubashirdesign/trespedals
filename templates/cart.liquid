<div class="bg-white">
    <div class="max-w-2xl mx-auto pt-16 pb-24 px-4 sm:px-6 lg:max-w-7xl lg:px-8">
        <h1 class="text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl">Shopping Cart</h1>
        {% if cart.item_count == 0 %}
        <!-- This example requires Tailwind CSS v2.0+ -->
<!--
  This example requires updating your template:

  ```
  <html class="h-full">
  <body class="h-full">
  ```
-->
<div class="bg-gray-200 min-h-full px-4 py-16 sm:px-6 sm:py-24 md:grid md:place-items-center lg:px-8">
    <div class="max-w-max mx-auto">
      <main class="sm:flex">
       
        <div class="sm:ml-6">
          <div class="sm:border-l sm:border-gray-200">
            <h1 class="text-4xl font-extrabold text-gray-900 tracking-tight sm:text-5xl text-center">Your Cart Is Empty!.</h1>
            <p class="mt-1 text-base text-gray-500 text-center">Please add stuff to the cart!</p>
          </div>
          <div class="mt-10 flex justify-center space-x-3 sm:border-l sm:border-transparent">
            <a href="/" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-primaryRed hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"> Go back home </a>
            <a href="/pages/contact" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-gray-900 bg-white hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"> Contact support </a>
          </div>
        </div>
      </main>
    </div>
  </div>
  
          {% else %}


        <div class="mt-12 lg:grid lg:grid-cols-12 lg:gap-x-12 lg:items-start xl:gap-x-16">
        <div class="lg:col-span-7">
            {% section 'main-cart-items' %}
        </div>
        <div class="mt-16 bg-gray-50 rounded-lg px-4 py-6 sm:p-6 lg:p-8 lg:mt-0 lg:col-span-5 cart__footer-wrapper">
            {% section 'main-cart-footer' %}
        </div>
        </div>
        {% endif %}
    </div>
</div>
