<svg class="flex-shrink-0 h-6 w-6 text-lightPrimaryBlue group-hover:text-black" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
  <path stroke-linecap="round" stroke-linejoin="round" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
</svg>

<span class="visually-hidden">{{ 'templates.cart.cart' | t }}</span>
{%- if cart != empty -%}
<span class="sr-only">items in cart, view bag</span>
  <div class="cart-count-bubble">
      {%- if cart.item_count < 100 -%}
      <span class="ml-2 text-sm font-medium text-lightPrimaryBlue group-hover:text-black">{{ cart.item_count }}</span>

        <span aria-hidden="true"></span>
      {%- endif -%}
      <span class="visually-hidden">{{ 'sections.header.cart_count' | t: count: cart.item_count }}</span>
  </div>
{%- endif -%}
